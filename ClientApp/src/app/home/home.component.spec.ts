import { HomeComponent } from './home.component';

fdescribe('figuring out how testing works', () => {

  it('does it do the thing?', () => {
    const a = 3;
    expect(a).toBeDefined();
  });
});


